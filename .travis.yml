language:
    - c
dist:
    - trusty
compiler:
    - gcc
    - clang
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y autoconf-archive
install:
    - autoreconf --install
    - ./configure
    - make -j4 V=1

